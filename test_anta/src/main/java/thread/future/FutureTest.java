package thread.future;

import org.junit.jupiter.api.Test;

import java.util.concurrent.ExecutionException;
import java.util.concurrent.Future;

/**
 * 内容描述：
 * 创建人：yang.liu
 * 创建时间：2019/7/10 0:37
 * 版本：1.0
 */
public class FutureTest {

    @Test
    public Boolean isServerTakingRequests(String server) throws InterruptedException {
//        ServerAcceptingRequestsVerifier acceptingRequestsVerifier = new ServerAcceptingRequestsVerifier();
//        Future future = THREAD_POOL.submit(acceptingRequestsVerifier);
//        try {
//            Boolean isAcceptingRequests = future.get();
//            //waits for the thread to complete, even if it hasn't started
//            return isAcceptingRequests;
//        } catch (ExecutionException e) {
//            throw new UnresponsiveException(e.getCause());
        //}
        return null;

    }

}
